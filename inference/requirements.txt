# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0

# Request validation
pydantic==2.7.1

# ML runtimes — both installed so the same image serves either model type
xgboost==2.0.3
torch==2.2.2          # CPU-only wheel specified in Dockerfile pip install
scikit-learn==1.4.1
numpy==1.26.4
pandas==2.2.1

# MLflow (model registry + artifact download)
mlflow==2.12.1
boto3==1.34.0

# Redis async client
redis[hiredis]==5.0.4    # hiredis C parser: 5-10x faster for large responses

# Observability
python-json-logger==2.0.7
